# PostgreSQL settings used by Docker Compose.
# Copy this file to .env.docker and replace the password.
# Never commit the real .env.docker file.

POSTGRES_DB=britmart_supplier
POSTGRES_USER=britmart_api
POSTGRES_PASSWORD=replace-with-a-secure-postgresql-password